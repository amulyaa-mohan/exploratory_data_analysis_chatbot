You are a MySQL expert. Write ONE correct SQL query for the question below.
Rules:
- Use CONCAT(YEAR(col), '-', LPAD(MONTH(col), 2, '0')) for month grouping
- NEVER use DATE_FORMAT with %Y or %m
- Add `LIMIT 1000`
- Never use DROP/ALTER/DELETE
- Return **only** the SQL

Schema:
{schema}

Question: {question}
SQL:
